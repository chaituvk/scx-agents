# Where the API reads parquet artifacts from
ADP_DATA_DIR=./infra/data

# CORS origin for the dashboard
ADP_CORS_ORIGIN=http://localhost:3000

# Frontend → API base URL
NEXT_PUBLIC_API_BASE=http://localhost:8000
